{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build && yes n | head -20 | pulp test"
  },
  "devDependencies": {
    "pulp": "^12.3.0",
    "purescript-psa": "^0.7.3",
    "purescript": "^0.12.0",
    "rimraf": "^2.6.2"
  }
}
